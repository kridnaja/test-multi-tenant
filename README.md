
**แนวคิดและการออกแบบระบบ (System Design Overview)**

โปรเจกต์นี้เป็น Backend-only system พัฒนาด้วย
Express.js, Prisma ORM, TypeScript และ Mysql

แนวคิดการออกแบบระบบและความถูกต้องของ business logic
มากกว่าการเลือกภาษา หรือ framework ใดเป็นพิเศษ

จึงออกแบบระบบโดยยึด Clean Architecture ร่วมกับ Domain-Driven Design (DDD)
โดยมี Business Rules เป็นศูนย์กลาง แทนการออกแบบที่ยึด database หรือ framework

โครงสร้างสถาปัตยกรรมระบบ

ระบบถูกแบ่งความรับผิดชอบออกเป็น layer อย่างชัดเจน:

Domain
กฎของธุรกิจและ business logic (Entities, Value Objects)

Application
Use cases และ workflow ของระบบ
ทำหน้าที่จัดลำดับการทำงานของ business logic

Infrastructure
Database, ORM, framework และการเชื่อมต่อภายนอก

ประโยชน์ของโครงสร้างนี้

Business logic ไม่ผูกติดกับ framework หรือ database

สามารถเปลี่ยน ORM หรือ database ได้โดยไม่กระทบ domain logic

โค้ดอ่านง่าย ดูแลรักษา และขยายต่อได้ในระยะยาว

Multi-Tenant Design & Tenant Isolation

ระบบถูกออกแบบให้รองรับ Multi-Tenant SaaS

ทุก entity ที่เกี่ยวข้องกับธุรกิจต้องมี tenantId

ข้อมูลของแต่ละ tenant ถูกแยกจากกันอย่างชัดเจน

ไม่สามารถเข้าถึงข้อมูลข้าม tenant ได้

การบังคับใช้ Tenant Isolation

Domain Level
Entity และ Repository ต้องใช้ tenantId เสมอ

Middleware Level
ดึง tenantId จาก request header และผูกกับ request ตลอด lifecycle

Database Level
ทุกตารางมี tenantId เป็นส่วนหนึ่งของ Primary Key หรือ Foreign Key

แนวทางนี้ช่วยลดความเสี่ยงเรื่อง data leakage และเพิ่มความน่าเชื่อถือของข้อมูล

Scalability Approach

ระบบถูกพัฒนาเป็น Version 1 โดยใช้แนวคิด
Modular Monolith + Clean Architecture + DDD

เหตุผล:

เริ่มจากโครงสร้างที่เรียบง่าย

ลดความซับซ้อนในการพัฒนาและดูแลรักษา

ทำให้ business logic ชัดเจนและตรวจสอบได้ง่าย

ในอนาคต ระบบสามารถพัฒนาไปสู่:

Database sharding

หรือ Microservices

โดยไม่ต้องรื้อหรือเขียน business logic ใหม่ทั้งหมด

**การตอบโจทย์ Core Requirements**
1. Multi-Tenant Model

รองรับหลายบริษัท (Multi-Tenant SaaS)

ผู้ใช้สังกัด tenant เดียวเท่านั้น

ไม่สามารถเข้าถึงข้อมูลของ tenant อื่นได้

แนวทางที่เลือกใช้:
Single Database + tenantId ในทุกตาราง
เพื่อความเรียบง่าย และรองรับการ scale ในอนาคต

2. Product & Inventory (Per Tenant)

แต่ละ tenant มี Product และ Inventory แยกจากกัน

Inventory ไม่สามารถติดลบได้

Business Rule (Domain Level):

inventory.decrease(quantity)


หากจำนวนที่ลดมากกว่าของที่มี → throw error
Domain จะไม่อนุญาตให้ inventory ติดลบในทุกกรณี

3. Sales Transaction

ขั้นตอนการขาย:

ตรวจสอบว่า Product อยู่ใน tenant เดียวกัน

ตรวจสอบ Inventory

หักจำนวนสินค้า

บันทึก Sale

ทุกขั้นตอนทำภายใต้ Database Transaction

prisma.$transaction(...)


หากขั้นตอนใดล้มเหลว ระบบจะ rollback ทั้งหมด
เพื่อป้องกันข้อมูลไม่สอดคล้องกัน

สรุปแนวคิดหลักของระบบ

ระบบนี้ถูกออกแบบโดยเน้น:

ความถูกต้องของ business logic

ความน่าเชื่อถือของข้อมูล

tenant isolation

โครงสร้างที่เข้าใจง่าย ขยายต่อได้ และอธิบายเหตุผลได้ชัดเจน



**Trade-offs & Limitations**
1. Single Database + tenantId

ข้อดี

โครงสร้างเรียบง่าย ดูแลง่าย

เหมาะกับระบบระยะเริ่มต้น

ข้อจำกัด

tenant ที่มี traffic สูงอาจกระทบ tenant อื่น

ต้องบังคับใช้ tenantId ทุก query อย่างเคร่งครัด

เลือกใช้เพื่อความเรียบง่ายของ Version 1 และสามารถพัฒนาไปสู่ sharding หรือ multi-database ได้ในอนาคต

2. Clean Architecture + DDD

ข้อดี

Business logic ชัดเจน ไม่ผูกกับ framework หรือ database

ลด bug และรองรับการเปลี่ยนแปลงในอนาคต

ข้อจำกัด

โครงสร้างซับซ้อนกว่า CRUD ปกติ

ใช้เวลา setup เริ่มต้นมากกว่า

3. Modular Monolith

ข้อดี

พัฒนาและ debug ง่าย

ไม่มี overhead ของ microservices

ข้อจำกัด

Scale ทีมใหญ่หรือ deploy แยก service ยังไม่ได้

ออกแบบให้สามารถแยกเป็น microservices ได้ภายหลัง โดยไม่กระทบ domain logic

4. Transaction Handling

ใช้ database transaction เพื่อป้องกันข้อมูลไม่สอดคล้อง

Domain ป้องกัน inventory ติดลบ

ข้อจำกัด

ยังไม่ได้จำลอง high concurrency หรือ race condition ระดับสูง

5. Authentication / Authorization

ยังไม่ได้ implement เพื่อโฟกัสที่ core business logic

tenantId ถูกส่งผ่าน header โดยตรง

สามารถเพิ่ม JWT และ role-based access ได้ในอนาคต

#########################################

🧪 Project Setup & Local Testing
1. Requirements

Node.js ≥ 18

MySQL

npm / pnpm / yarn

Setup Project
git clone https://github.com/kridnaja/test-multi-tenant.git
cd backend
npm install

สร้างไฟล์ .env

DATABASE_URL="mysql://user:password@localhost:3306/multi_tenant_db"
PORT=3000

รัน Prisma

npx prisma migrate dev
npx prisma generate


รัน server

npm run dev


ตรวจสอบ health check

curl http://localhost:3000/health

3. Mock Data (Seed ด้วย SQL)

เนื่องจากใน Version นี้มีเพียง POST /sales API
Product, Inventory และ Tenant จะถูก mock ด้วย SQL โดยตรง
เพื่อโฟกัสที่ business logic และ transaction consistency

3.1 สร้าง Tenant
INSERT INTO tenant (id, name)
VALUES ('tenant-1', 'Tenant A');

3.2 สร้าง Product (ผูกกับ tenant)
INSERT INTO product (id, tenantId, name, price)
VALUES ('product-1', 'tenant-1', 'Sample Product', 100);

3.3 สร้าง Inventory
INSERT INTO inventory (tenantId, productId, quantity)
VALUES ('tenant-1', 'product-1', 10);


🔒 Inventory ถูกผูกกับ tenant และ product โดยตรง
เพื่อป้องกันข้อมูลข้าม tenant

4. Test Sales API ด้วย curl
4.1 ยิงขายสินค้า (สำเร็จ)
curl -X POST http://localhost:3000/api/sales \
  -H "Content-Type: application/json" \
  -H "x-tenant-id: tenant-1" \
  -d '{
    "productId": "product-1",
    "quantity": 2
  }'

Response
{
  "id": "uuid",
  "tenantId": "tenant-1",
  "productId": "product-1",
  "quantity": 2,
  "price": 100,
  "total": 200
}

4.2 Inventory ไม่พอ (Error Case)
curl -X POST http://localhost:3000/api/sales \
  -H "Content-Type: application/json" \
  -H "x-tenant-id: tenant-1" \
  -d '{
    "productId": "product-1",
    "quantity": 100
  }'

Response
{
  "message": "Insufficient stock"
}


❌ ระบบไม่อนุญาตให้ inventory ติดลบ
❌ Transaction ถูก rollback ทั้งหมด ไม่ทิ้งข้อมูลที่ไม่สอดคล้องกันไว้

3. Mock Data (Seed ด้วย SQL)

เนื่องจากใน Version นี้มีเพียง POST /sales API
Product, Inventory และ Tenant จะถูก mock ด้วย SQL โดยตรง
เพื่อโฟกัสที่ business logic และ transaction consistency

3.1 สร้าง Tenant
INSERT INTO tenants (id, name)
VALUES ('tenant-1', 'Tenant A');

3.2 สร้าง Product (ผูกกับ tenant)
INSERT INTO products (id, tenant_id, name, price)
VALUES ('product-1', 'tenant-1', 'Sample Product', 100);

3.3 สร้าง Inventory
INSERT INTO inventories (id, tenant_id, product_id, quantity)
VALUES ('inventory-1', 'tenant-1', 'product-1', 10);


🔒 Inventory ถูกผูกกับ tenant และ product โดยตรง
เพื่อป้องกันข้อมูลข้าม tenant

4. Test Sales API ด้วย curl
4.1 ยิงขายสินค้า (สำเร็จ)
curl -X POST http://localhost:3000/api/sales \
  -H "Content-Type: application/json" \
  -H "x-tenant-id: tenant-1" \
  -d '{
    "productId": "product-1",
    "quantity": 2
  }'

Response
{
  "id": "uuid",
  "tenantId": "tenant-1",
  "productId": "product-1",
  "quantity": 2,
  "price": 100,
  "total": 200
}

4.2 Inventory ไม่พอ (Error Case)
curl -X POST http://localhost:3000/api/sales \
  -H "Content-Type: application/json" \
  -H "x-tenant-id: tenant-1" \
  -d '{
    "productId": "product-1",
    "quantity": 100
  }'

Response
{
  "message": "Inventory is not enough"
}


❌ ระบบไม่อนุญาตให้ inventory ติดลบ
❌ Transaction ถูก rollback ทั้งหมด
